# configs/default.yaml
training:
  epochs: 5
  batch_size: 32
  learning_rate: 0.001
  optimizer:
    _target_: torch.optim.Adam
    lr: ${training.learning_rate}

model:
  input_size: 784
  hidden_size: 128
  dropout: 0.2
  activation: "relu"